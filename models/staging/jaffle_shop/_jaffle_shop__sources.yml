version: 2

sources:
  - name: 'jaffle_shop'
    database: raw
    schema: jaffle_shop
    tables:
      - name: customers

      - name: orders
        freshness: 
          error_after:
            count: 12
            period: hour
          warn_after:
            count: 6
            period: hour
        loaded_at_field: _etl_loaded_at